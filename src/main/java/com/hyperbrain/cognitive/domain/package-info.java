/**
 * Cognitive domain: iaService contracts — read-only query ports to other modules.
 * This module never modifies state in other modules directly; it publishes events
 * or calls application services.
 */
package com.hyperbrain.cognitive.domain;
