/**
 * Prioritizer outbound ports: read the aggregate state that feeds the Priority Score and persist
 * the computed scores back to {@code core_executable} (#6a).
 */
package com.hyperbrain.prioritizer.domain.port.out;
