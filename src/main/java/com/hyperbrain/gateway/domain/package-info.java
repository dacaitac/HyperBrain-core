/**
 * Gateway domain: webhook signature verification contracts and inbound event models.
 * Gateway only invokes application services of the core module; it has no access
 * to sync, finance, learning, cognitive, prioritizer, or planner internals.
 */
package com.hyperbrain.gateway.domain;
