/**
 * Outbound ports (secondary ports) for the sync module: repository interfaces that the domain
 * depends on and the infrastructure layer implements.
 */
package com.hyperbrain.sync.domain.port.out;
